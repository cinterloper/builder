export GITHUB_TOKEN=$(cat ~/.ghtoken )
export GITHUB_USER=cinterloper
#building builder
docker run -t -i -v /tmp:/tmp -v $(which docker):$(which docker) -v $(realpath ~/.m2):/root/.m2 -v $(realpath ~/.aws):/root/.aws -v /var/run/docker.sock:/var/run/docker.sock -v $(realpath ~/.docker/):/root/.docker -e BUILDARGS -e GH_USER -e GH_TOKEN -e GH_REPO=cinterloper/builder cinterloper/builder
