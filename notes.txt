export GH_TOKEN=$(cat ~/.ghtoken )
export GH_USER=cinterloper
docker run -t -i -v $(realpath ~/.m2):/root/.m2 -v $(realpath ~/.aws):/root/.aws -e GH_USER -e GH_TOKEN -e GH_REPO=cinterloper/cornerstone

